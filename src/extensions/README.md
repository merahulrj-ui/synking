# Synkin Extensions & Modular Additions

This directory is designated for all future feature additions, new screens, services, or utilities.

## Core Rule:
- The existing codebase (commit `64bcb09` / `v1.0.71-golden-core-locked`) is strictly **IMMUTABLE**.
- Never edit, modify, or delete files in the core codebase.
- Any new features must be developed as modular extensions inside this folder or in separate files.
